@{
    ViewData["Title"] = "Cadastro Realizado com Sucesso!";
    ViewData["Description"] = "Seu cadastro foi realizado com sucesso. Você receberá notificações das nossas lives.";
}

<!-- Hero Section with Animation -->
<section class="hero-section-success">
    <div class="container">
        <div class="success-content">
            <!-- Success Animation -->
            <div class="success-animation">
                <div class="checkmark-circle">
                    <div class="checkmark"></div>
                </div>
                <div class="celebration-emoji">🎉</div>
            </div>

            <!-- Main Content -->
            <div class="success-main">
                <h1 class="success-title">
                    Olá, @ViewBag.Nome! 👋
                </h1>
                <p class="success-subtitle">
                    Seu cadastro foi realizado com sucesso e você já está na nossa lista de interessados!
                </p>

                <!-- Status Cards -->
                <div class="status-grid">
                    <!-- Contatos Cadastrados -->
                    <div class="status-card">
                        <div class="status-icon">📱</div>
                        <h3 class="status-title">Contatos Cadastrados:</h3>
                        <div class="contact-list">
                            @if (!string.IsNullOrWhiteSpace(ViewBag.Email))
                            {
                                <div class="contact-item">
                                    <span class="contact-icon">📧</span>
                                    <span class="contact-text">@ViewBag.Email</span>
                                    <span class="contact-status verified">✓</span>
                                </div>
                            }
                            @if (!string.IsNullOrWhiteSpace(ViewBag.WhatsApp))
                            {
                                <div class="contact-item">
                                    <span class="contact-icon">📱</span>
                                    <span class="contact-text">@ViewBag.WhatsApp</span>
                                    <span class="contact-status verified">✓</span>
                                </div>
                            }
                        </div>
                    </div>

                    <!-- O que acontece agora -->
                    <div class="status-card">
                        <div class="status-icon">🚀</div>
                        <h3 class="status-title">O que acontece agora:</h3>
                        <div class="timeline">
                            <div class="timeline-item">
                                <div class="timeline-marker active">1</div>
                                <div class="timeline-content">
                                    <h4>Boas-vindas enviadas</h4>
                                    <p>Você receberá uma mensagem de boas-vindas nos contatos cadastrados</p>
                                </div>
                            </div>
                            <div class="timeline-item">
                                <div class="timeline-marker">2</div>
                                <div class="timeline-content">
                                    <h4>Notificações de lives</h4>
                                    <p>Sempre que formos ao vivo, você será notificado</p>
                                </div>
                            </div>
                            <div class="timeline-item">
                                <div class="timeline-marker">3</div>
                                <div class="timeline-content">
                                    <h4>Conteúdo exclusivo</h4>
                                    <p>Dicas, materiais e informações sobre tecnologia e carreira</p>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Tips Section -->
                <div class="tips-section">
                    <h3 class="section-title">💡 Enquanto isso, você pode:</h3>
                    <div class="tips-grid">
                        <div class="tip-item">
                            <div class="tip-icon">🔔</div>
                            <h4>Ativar notificações</h4>
                            <p>Nos nossos canais para não perder nenhuma live</p>
                        </div>
                        <div class="tip-item">
                            <div class="tip-icon">👥</div>
                            <h4>Compartilhar</h4>
                            <p>Com amigos que também programam</p>
                        </div>
                        <div class="tip-item">
                            <div class="tip-icon">💬</div>
                            <h4>Participar</h4>
                            <p>Dos chats durante as lives ao vivo</p>
                        </div>
                        <div class="tip-item">
                            <div class="tip-icon">📚</div>
                            <h4>Explorar</h4>
                            <p>Nosso conteúdo sobre carreira tech</p>
                        </div>
                    </div>
                </div>

                <!-- Action Buttons -->
                <div class="action-buttons">
                    <a href="https://youtube.com/@@diogocostadev" target="_blank" class="btn-action primary">
                        <span class="btn-icon">📺</span>
                        Acessar YouTube
                    </a>
                    <a href="https://twitch.tv/diogocostadev" target="_blank" class="btn-action secondary">
                        <span class="btn-icon">🎮</span>
                        Acessar Twitch
                    </a>
                    <a href="@Url.Action("Index", "Home")" class="btn-action tertiary">
                        <span class="btn-icon">🏠</span>
                        Voltar ao Site
                    </a>
                </div>

                <!-- Share Section -->
                <div class="share-section">
                    <p class="share-text">Gostou? Compartilhe com seus amigos:</p>
                    <div class="share-buttons">
                        <button class="share-btn whatsapp" onclick="shareOnWhatsApp()">
                            <span>📱</span>
                            WhatsApp
                        </button>
                        <button class="share-btn twitter" onclick="shareOnTwitter()">
                            <span>🐦</span>
                            Twitter
                        </button>
                        <button class="share-btn linkedin" onclick="shareOnLinkedIn()">
                            <span>💼</span>
                            LinkedIn
                        </button>
                        <button class="share-btn copy" onclick="copyLink()">
                            <span>📋</span>
                            Copiar Link
                        </button>
                    </div>
                </div>

            </div>
        </div>
    </div>
</section>

<style>
/* Página de Sucesso - Design Moderno */
.hero-section-success {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    min-height: 100vh;
    padding: 2rem 0;
    color: white;
}

.success-content {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 1rem;
}

/* Animação de Sucesso */
.success-animation {
    text-align: center;
    margin-bottom: 3rem;
    position: relative;
}

.checkmark-circle {
    width: 80px;
    height: 80px;
    border-radius: 50%;
    background: linear-gradient(45deg, #4ade80, #22c55e);
    margin: 0 auto 1rem;
    display: flex;
    align-items: center;
    justify-content: center;
    animation: bounceIn 0.8s ease-out;
    box-shadow: 0 10px 30px rgba(34, 197, 94, 0.3);
}

.checkmark {
    width: 30px;
    height: 15px;
    border: 4px solid white;
    border-top: none;
    border-right: none;
    transform: rotate(-45deg);
    animation: checkmark 0.6s ease-in-out 0.3s both;
}

.celebration-emoji {
    font-size: 3rem;
    animation: bounce 2s infinite;
    position: absolute;
    top: -20px;
    right: calc(50% - 60px);
}

@@keyframes bounceIn {
    0% { transform: scale(0.3); opacity: 0; }
    50% { transform: scale(1.05); }
    70% { transform: scale(0.9); }
    100% { transform: scale(1); opacity: 1; }
}

@@keyframes checkmark {
    0% { width: 0; height: 0; }
    25% { width: 0; height: 15px; }
    50% { width: 30px; height: 15px; }
}

@@keyframes bounce {
    0%, 20%, 50%, 80%, 100% { transform: translateY(0); }
    40% { transform: translateY(-10px); }
    60% { transform: translateY(-5px); }
}

/* Conteúdo Principal */
.success-main {
    background: rgba(255, 255, 255, 0.1);
    backdrop-filter: blur(20px);
    border-radius: 20px;
    padding: 3rem;
    border: 1px solid rgba(255, 255, 255, 0.2);
}

.success-title {
    font-size: 2.5rem;
    font-weight: 700;
    text-align: center;
    margin-bottom: 1rem;
    background: linear-gradient(45deg, #fbbf24, #f59e0b);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
}

.success-subtitle {
    font-size: 1.25rem;
    text-align: center;
    margin-bottom: 3rem;
    opacity: 0.9;
    line-height: 1.6;
}

/* Status Grid */
.status-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(400px, 1fr));
    gap: 2rem;
    margin-bottom: 3rem;
}

.status-card {
    background: rgba(255, 255, 255, 0.15);
    border-radius: 15px;
    padding: 2rem;
    border: 1px solid rgba(255, 255, 255, 0.2);
    transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.status-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
}

.status-icon {
    font-size: 2rem;
    margin-bottom: 1rem;
}

.status-title {
    font-size: 1.25rem;
    font-weight: 600;
    margin-bottom: 1.5rem;
    color: white;
}

/* Lista de Contatos */
.contact-list {
    display: flex;
    flex-direction: column;
    gap: 1rem;
}

.contact-item {
    display: flex;
    align-items: center;
    gap: 1rem;
    padding: 1rem;
    background: rgba(255, 255, 255, 0.1);
    border-radius: 10px;
    border: 1px solid rgba(255, 255, 255, 0.1);
}

.contact-icon {
    font-size: 1.25rem;
}

.contact-text {
    flex: 1;
    font-weight: 500;
}

.contact-status.verified {
    color: #4ade80;
    font-weight: 600;
    font-size: 1.25rem;
}

/* Timeline */
.timeline {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
}

.timeline-item {
    display: flex;
    align-items: flex-start;
    gap: 1rem;
}

.timeline-marker {
    width: 2.5rem;
    height: 2.5rem;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.2);
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 600;
    font-size: 0.9rem;
    border: 2px solid rgba(255, 255, 255, 0.3);
    transition: all 0.3s ease;
    flex-shrink: 0;
}

.timeline-marker.active {
    background: linear-gradient(45deg, #4ade80, #22c55e);
    border-color: #4ade80;
    color: white;
    transform: scale(1.1);
    box-shadow: 0 4px 15px rgba(34, 197, 94, 0.3);
}

.timeline-content h4 {
    font-weight: 600;
    margin-bottom: 0.5rem;
    color: white;
}

.timeline-content p {
    opacity: 0.8;
    line-height: 1.5;
}

/* Seções */
.section-title {
    font-size: 1.5rem;
    font-weight: 600;
    margin-bottom: 2rem;
    text-align: center;
    color: white;
}

.channels-section {
    margin-bottom: 3rem;
}

.channels-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 1.5rem;
}

.channel-card {
    display: flex;
    align-items: center;
    gap: 1rem;
    padding: 1.5rem;
    background: rgba(255, 255, 255, 0.1);
    border-radius: 15px;
    border: 1px solid rgba(255, 255, 255, 0.2);
    text-decoration: none;
    color: white;
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
}

.channel-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.1), transparent);
    transition: left 0.5s;
}

.channel-card:hover::before {
    left: 100%;
}

.channel-card:hover {
    transform: translateY(-3px);
    box-shadow: 0 15px 30px rgba(0, 0, 0, 0.2);
}

.channel-card.youtube:hover {
    border-color: #ff0000;
    box-shadow: 0 15px 30px rgba(255, 0, 0, 0.3);
}

.channel-card.twitch:hover {
    border-color: #9146ff;
    box-shadow: 0 15px 30px rgba(145, 70, 255, 0.3);
}

.channel-icon {
    font-size: 2rem;
}

.channel-info {
    flex: 1;
}

.channel-info h4 {
    font-size: 1.25rem;
    font-weight: 600;
    margin-bottom: 0.5rem;
}

.channel-info p {
    opacity: 0.8;
    margin-bottom: 0.5rem;
}

.channel-url {
    font-size: 0.9rem;
    opacity: 0.7;
    font-family: monospace;
}

.channel-action {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-weight: 500;
    opacity: 0.8;
}

/* Tips Section */
.tips-section {
    margin-bottom: 3rem;
}

.tips-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 1.5rem;
}

.tip-item {
    text-align: center;
    padding: 1.5rem;
    background: rgba(255, 255, 255, 0.1);
    border-radius: 15px;
    border: 1px solid rgba(255, 255, 255, 0.2);
    transition: transform 0.3s ease;
}

.tip-item:hover {
    transform: translateY(-5px);
}

.tip-icon {
    font-size: 2.5rem;
    margin-bottom: 1rem;
}

.tip-item h4 {
    font-weight: 600;
    margin-bottom: 0.5rem;
    color: white;
}

.tip-item p {
    opacity: 0.8;
    line-height: 1.5;
}

/* Action Buttons */
.action-buttons {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
    justify-content: center;
    margin-bottom: 3rem;
}

.btn-action {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 1rem 2rem;
    border-radius: 12px;
    text-decoration: none;
    font-weight: 600;
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
}

.btn-action.primary {
    background: linear-gradient(45deg, #ef4444, #dc2626);
    color: white;
    border: 2px solid #ef4444;
}

.btn-action.secondary {
    background: linear-gradient(45deg, #8b5cf6, #7c3aed);
    color: white;
    border: 2px solid #8b5cf6;
}

.btn-action.tertiary {
    background: rgba(255, 255, 255, 0.1);
    color: white;
    border: 2px solid rgba(255, 255, 255, 0.3);
}

.btn-action:hover {
    transform: translateY(-2px);
    box-shadow: 0 10px 25px rgba(0, 0, 0, 0.2);
}

.btn-icon {
    font-size: 1.25rem;
}

/* Share Section */
.share-section {
    text-align: center;
    margin-bottom: 3rem;
}

.share-text {
    margin-bottom: 1.5rem;
    opacity: 0.9;
    font-size: 1.1rem;
}

.share-buttons {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
    justify-content: center;
}

.share-btn {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.75rem 1.5rem;
    border-radius: 10px;
    border: none;
    background: rgba(255, 255, 255, 0.1);
    color: white;
    font-weight: 500;
    cursor: pointer;
    transition: all 0.3s ease;
    border: 1px solid rgba(255, 255, 255, 0.2);
}

.share-btn:hover {
    background: rgba(255, 255, 255, 0.2);
    transform: translateY(-2px);
}

.share-btn.whatsapp:hover { border-color: #25d366; }
.share-btn.twitter:hover { border-color: #1da1f2; }
.share-btn.linkedin:hover { border-color: #0077b5; }
.share-btn.copy:hover { border-color: #6b7280; }

/* Footer */
.success-footer {
    text-align: center;
    padding-top: 2rem;
    border-top: 1px solid rgba(255, 255, 255, 0.2);
}

.footer-text {
    opacity: 0.8;
    line-height: 1.6;
}

/* Responsive */
@@media (max-width: 768px) {
    .success-main {
        padding: 2rem 1.5rem;
    }
    
    .success-title {
        font-size: 2rem;
    }
    
    .status-grid {
        grid-template-columns: 1fr;
    }
    
    .channels-grid {
        grid-template-columns: 1fr;
    }
    
    .tips-grid {
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    }
    
    .action-buttons {
        flex-direction: column;
        align-items: center;
    }
    
    .btn-action {
        width: 100%;
        max-width: 300px;
        justify-content: center;
    }
    
    .share-buttons {
        flex-direction: column;
        align-items: center;
    }
    
    .share-btn {
        width: 100%;
        max-width: 200px;
        justify-content: center;
    }
}
</style>

<script>
// Funcionalidades de compartilhamento
function shareOnWhatsApp() {
    const text = "Acabei de me cadastrar para receber notificações das lives do Diogo Costa! 🎉 Lives sobre programação, tecnologia e carreira tech. Cadastre-se também!";
    const url = window.location.origin + "/lives";
    const whatsappUrl = `https://wa.me/?text=${encodeURIComponent(text + "\n\n" + url)}`;
    window.open(whatsappUrl, '_blank');
}

function shareOnTwitter() {
    const text = "Acabei de me cadastrar para receber notificações das lives do @@diogocostadev! 🎉 Lives sobre programação, tecnologia e carreira tech.";
    const url = window.location.origin + "/lives";
    const twitterUrl = `https://twitter.com/intent/tweet?text=${encodeURIComponent(text)}&url=${encodeURIComponent(url)}`;
    window.open(twitterUrl, '_blank');
}

function shareOnLinkedIn() {
    const url = window.location.origin + "/lives";
    const linkedinUrl = `https://www.linkedin.com/sharing/share-offsite/?url=${encodeURIComponent(url)}`;
    window.open(linkedinUrl, '_blank');
}

async function copyLink() {
    const url = window.location.origin + "/lives";
    try {
        await navigator.clipboard.writeText(url);
        
        // Feedback visual
        const btn = event.target.closest('.share-btn');
        const originalText = btn.innerHTML;
        btn.innerHTML = '<span>✅</span> Link Copiado!';
        btn.style.background = 'rgba(34, 197, 94, 0.3)';
        btn.style.borderColor = '#22c55e';
        
        setTimeout(() => {
            btn.innerHTML = originalText;
            btn.style.background = '';
            btn.style.borderColor = '';
        }, 2000);
    } catch (err) {
        // Fallback para navegadores mais antigos
        const textArea = document.createElement('textarea');
        textArea.value = url;
        document.body.appendChild(textArea);
        textArea.select();
        document.execCommand('copy');
        document.body.removeChild(textArea);
        
        alert('Link copiado para a área de transferência!');
    }
}

// Animação de entrada dos elementos
document.addEventListener('DOMContentLoaded', function() {
    // Animar cards com delay
    const cards = document.querySelectorAll('.status-card, .channel-card, .tip-item');
    cards.forEach((card, index) => {
        card.style.opacity = '0';
        card.style.transform = 'translateY(20px)';
        
        setTimeout(() => {
            card.style.transition = 'all 0.6s ease';
            card.style.opacity = '1';
            card.style.transform = 'translateY(0)';
        }, 100 * index);
    });
    
    // Animar timeline markers
    setTimeout(() => {
        const markers = document.querySelectorAll('.timeline-marker');
        markers.forEach((marker, index) => {
            setTimeout(() => {
                marker.classList.add('active');
            }, 500 * index);
        });
    }, 1000);
});
</script> 