@model DesbloqueioModel
@{
    ViewData["Title"] = "Manual da Primeira Virada - Quebre o Ciclo da Autossabotagem";
}

<div class="bg-white min-h-screen">
    <!-- Hero Section -->
    <div class="bg-gradient-to-br from-blue-900 to-blue-700 text-white py-20">
        <div class="container mx-auto px-4 max-w-4xl text-center">
            <h1 class="text-5xl md:text-6xl font-serif mb-6">
                Manual da Primeira Virada
            </h1>
            <p class="text-2xl md:text-3xl mb-8 text-blue-100">
                Quebre o ciclo da autossabotagem e comece hoje sua jornada de transformação
            </p>
            <div class="bg-yellow-400 text-black px-6 py-3 rounded-lg inline-block mb-8 font-bold text-lg">
                🎁 TOTALMENTE GRATUITO
            </div>
        </div>
    </div>

    <!-- Problem Section -->
    <div class="py-16 bg-gray-50">
        <div class="container mx-auto px-4 max-w-4xl">
            <div class="text-center mb-12">
                <h2 class="text-4xl font-serif mb-6">Você já se perguntou por que...</h2>
            </div>
            <div class="grid md:grid-cols-2 gap-8">
                <div class="bg-white p-6 rounded-lg shadow-sm border-l-4 border-red-500">
                    <div class="text-red-500 text-2xl mb-3">😤</div>
                    <h3 class="text-xl font-semibold mb-3">Você sabe exatamente o que precisa fazer...</h3>
                    <p class="text-gray-600">Mas sempre encontra uma desculpa para não começar ou desiste no meio do caminho?</p>
                </div>
                <div class="bg-white p-6 rounded-lg shadow-sm border-l-4 border-red-500">
                    <div class="text-red-500 text-2xl mb-3">🔄</div>
                    <h3 class="text-xl font-semibold mb-3">Você tem ideias brilhantes...</h3>
                    <p class="text-gray-600">Mas sempre arranja uma forma de sabotá-las antes mesmo de começar?</p>
                </div>
                <div class="bg-white p-6 rounded-lg shadow-sm border-l-4 border-red-500">
                    <div class="text-red-500 text-2xl mb-3">😟</div>
                    <h3 class="text-xl font-semibold mb-3">Você se compara com outros...</h3>
                    <p class="text-gray-600">E sempre se sente atrasado, como se nunca fosse capaz de alcançar seus objetivos?</p>
                </div>
                <div class="bg-white p-6 rounded-lg shadow-sm border-l-4 border-red-500">
                    <div class="text-red-500 text-2xl mb-3">⏰</div>
                    <h3 class="text-xl font-semibold mb-3">Você procrastina constantemente...</h3>
                    <p class="text-gray-600">E depois se culpa por não ser produtivo o suficiente?</p>
                </div>
            </div>
        </div>
    </div>

    <!-- Solution Section -->
    <div class="py-16 bg-white">
        <div class="container mx-auto px-4 max-w-4xl text-center">
            <h2 class="text-4xl font-serif mb-8">A resposta está na sua <span class="text-blue-600">mentalidade</span></h2>
            <p class="text-xl text-gray-600 mb-12">
                Não é falta de conhecimento técnico, não é falta de oportunidades. 
                O que te impede é um padrão mental que você pode quebrar <strong>hoje mesmo</strong>.
            </p>
            
            <div class="bg-blue-50 p-8 rounded-lg mb-12">
                <div class="text-blue-600 text-4xl mb-4">🧠</div>
                <h3 class="text-2xl font-semibold mb-4">O Manual da Primeira Virada vai te ensinar:</h3>
                <div class="grid md:grid-cols-2 gap-6 text-left">
                    <div class="flex items-start space-x-3">
                        <div class="text-green-500 text-xl">✓</div>
                        <p>Como identificar os padrões de autossabotagem que te impedem de progredir</p>
                    </div>
                    <div class="flex items-start space-x-3">
                        <div class="text-green-500 text-xl">✓</div>
                        <p>Técnicas práticas para quebrar o ciclo da procrastinação</p>
                    </div>
                    <div class="flex items-start space-x-3">
                        <div class="text-green-500 text-xl">✓</div>
                        <p>Como transformar sua mentalidade de "empregado" para "empreendedor"</p>
                    </div>
                    <div class="flex items-start space-x-3">
                        <div class="text-green-500 text-xl">✓</div>
                        <p>Estratégias para manter a consistência em seus projetos</p>
                    </div>
                    <div class="flex items-start space-x-3">
                        <div class="text-green-500 text-xl">✓</div>
                        <p>Como lidar com o medo do fracasso e da rejeição</p>
                    </div>
                    <div class="flex items-start space-x-3">
                        <div class="text-green-500 text-xl">✓</div>
                        <p>O método para dar sua primeira virada mental hoje</p>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- CTA Form Section -->
    <div class="py-16 bg-gradient-to-br from-blue-600 to-blue-800 text-white">
        <div class="container mx-auto px-4 max-w-2xl text-center">
            <h2 class="text-4xl font-serif mb-6">Baixe gratuitamente o Manual da Primeira Virada</h2>
            <p class="text-xl mb-8 text-blue-100">
                Insira seus dados abaixo e receba imediatamente o PDF por email
            </p>
            
            <div class="bg-white rounded-lg p-8 text-left" id="form">
                <form asp-action="Cadastrar" method="post" class="space-y-6" data-submit-time="">
                    <!-- Honeypot fields - hidden from humans but visible to bots -->
                    <div style="position: absolute; left: -9999px; opacity: 0;" aria-hidden="true">
                        <input type="text" name="website" placeholder="Website" tabindex="-1" autocomplete="off" />
                        <input type="text" name="phone" placeholder="Phone" tabindex="-1" autocomplete="off" />
                        <input type="email" name="email_confirm" placeholder="Confirm Email" tabindex="-1" autocomplete="off" />
                    </div>
                    
                    <div>
                        <label asp-for="Nome" class="block text-sm font-medium text-gray-700 mb-2">Seu nome completo *</label>
                        <input asp-for="Nome" class="w-full px-4 py-3 border border-gray-300 rounded-md text-lg text-gray-900 bg-white focus:ring-2 focus:ring-blue-500 focus:border-transparent" 
                               placeholder="Digite seu nome completo" required />
                        <span asp-validation-for="Nome" class="text-red-500 text-sm block mt-1"></span>
                    </div>
                    
                    <div>
                        <label asp-for="Email" class="block text-sm font-medium text-gray-700 mb-2">Seu melhor email *</label>
                        <input asp-for="Email" type="email" class="w-full px-4 py-3 border border-gray-300 rounded-md text-lg text-gray-900 bg-white focus:ring-2 focus:ring-blue-500 focus:border-transparent" 
                               placeholder="seu@melhor-email.com" required />
                        <span asp-validation-for="Email" class="text-red-500 text-sm block mt-1"></span>
                    </div>
                    
                    <button type="submit" class="w-full bg-yellow-400 hover:bg-yellow-500 text-black font-bold py-4 px-6 rounded-md text-lg transition duration-200 transform hover:scale-105">
                        🚀 QUERO RECEBER O MANUAL AGORA
                    </button>
                    
                    <p class="text-xs text-gray-500 text-center mt-4">
                        Ao clicar no botão, você concorda em receber emails da nossa newsletter. 
                        Você pode cancelar a qualquer momento.
                    </p>
                </form>
            </div>
        </div>
    </div>

    <!-- Social Proof Section -->
    <div class="py-16 bg-gray-50">
        <div class="container mx-auto px-4 max-w-4xl text-center">
            <h2 class="text-3xl font-serif mb-12">Mais de 1.000 pessoas já baixaram</h2>
            
            <div class="bg-white p-6 rounded-lg shadow-sm mb-8">
                <p class="text-lg italic text-gray-600 mb-4">
                    "Finalmente entendi por que sempre sabotava meus projetos. O manual me deu clareza sobre os padrões mentais que me impediam de evoluir."
                </p>
                <div class="text-sm text-gray-500">— Carlos S., Desenvolvedor Full Stack</div>
            </div>
            
            <div class="bg-white p-6 rounded-lg shadow-sm mb-8">
                <p class="text-lg italic text-gray-600 mb-4">
                    "Apliquei as técnicas do manual e consegui lançar meu primeiro produto digital em 30 dias. Mudou completamente minha perspectiva."
                </p>
                <div class="text-sm text-gray-500">— Ana P., Desenvolvedora Frontend</div>
            </div>
        </div>
    </div>

    <!-- About Author Section -->
    <div class="py-16 bg-white">
        <div class="container mx-auto px-4 max-w-4xl">
            <div class="flex flex-col md:flex-row items-center gap-8">
                <div class="flex-shrink-0">
                    <img src="~/img/IMG_0045.webp" 
                         alt="Diogo Costa" 
                         class="w-32 h-32 rounded-full shadow-lg object-cover"
                         width="128" 
                         height="128"
                         style="aspect-ratio: 1; object-fit: cover;"
                         loading="lazy">
                </div>
                <div>
                    <h3 class="text-2xl font-semibold mb-4">Sobre o autor</h3>
                    <p class="text-gray-600 mb-4">
                        <strong>Diogo Costa</strong> é MVP Microsoft, desenvolvedor há mais de 20 anos e criador do método DC360. 
                        Já trabalhou com grandes empresas como Microsoft, Itaú, Mercedes-Benz e muitas outras.
                    </p>
                    <p class="text-gray-600">
                        Hoje ajuda programadores a transformarem suas habilidades técnicas em negócios lucrativos, 
                        conquistando liberdade profissional e financeira.
                    </p>
                </div>
            </div>
        </div>
    </div>

    <!-- Final CTA -->
    <div class="py-16 bg-blue-900 text-white text-center">
        <div class="container mx-auto px-4 max-w-2xl">
            <h2 class="text-3xl font-serif mb-6">Não perca mais tempo sabotando seus sonhos</h2>
            <p class="text-xl mb-8">Baixe agora o Manual da Primeira Virada e comece sua transformação hoje</p>
            <a href="#form" class="bg-yellow-400 hover:bg-yellow-500 text-black font-bold py-4 px-8 rounded-md text-lg transition duration-200 transform hover:scale-105 inline-block">
                BAIXAR MANUAL GRATUITO
            </a>
        </div>
    </div>
</div>

<script>
// Smooth scroll para âncoras
document.querySelectorAll('a[href^="#"]').forEach(anchor => {
    anchor.addEventListener('click', function (e) {
        e.preventDefault();
        const target = document.querySelector(this.getAttribute('href'));
        if (target) {
            target.scrollIntoView({
                behavior: 'smooth',
                block: 'center'
            });
        }
    });
});

// Anti-bot protection
document.addEventListener('DOMContentLoaded', function() {
    const form = document.querySelector('form[data-submit-time]');
    const startTime = Date.now();
    
    if (form) {
        form.addEventListener('submit', function(e) {
            const submitTime = Date.now();
            const timeSpent = submitTime - startTime;
            
            // Verificar honeypot fields
            const honeypotFields = ['website', 'phone', 'email_confirm'];
            for (const fieldName of honeypotFields) {
                const field = form.querySelector(`input[name="${fieldName}"]`);
                if (field && field.value.trim() !== '') {
                    console.log('Bot detected: honeypot field filled');
                    e.preventDefault();
                    alert('Erro no formulário. Tente novamente.');
                    return false;
                }
            }
            
            // Verificar se foi muito rápido (menos de 3 segundos)
            if (timeSpent < 3000) {
                console.log('Bot detected: too fast submission');
                e.preventDefault();
                alert('Por favor, preencha o formulário com cuidado.');
                return false;
            }
            
            // Adicionar timestamp ao formulário
            form.setAttribute('data-submit-time', submitTime.toString());
        });
    }
});
</script>
